## Async TODO list web app
##### Allows to add, edit, delete, update and track tasks and notes
##### python-sanic + docker + postresql

To run docker app with related database container:
```
docker-compose build
docker-compose up -d
```